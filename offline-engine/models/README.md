# Offline model directory

Place pre-downloaded Hugging Face model files here before runtime.

Required model paths:

- `offline-engine/models/openai/whisper-tiny/`
- `offline-engine/models/Xenova/m2m100_418M/`

Runtime auto-download is disabled. The local engine will fail fast if these folders are missing.
